---
phase: 02-multiplayer-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/layout.tsx
  - frontend/app/globals.css
  - frontend/app/page.tsx
  - frontend/tailwind.config.ts
autonomous: true
requirements:
  - MULTI-01

must_haves:
  truths:
    - "Home page renders INITIALIZE_OPERATOR setup screen with KINETIC_GRID cyber-noir aesthetic"
    - "Space Grotesk and Inter fonts load; Material Symbols Outlined available"
    - "Tailwind config has all KINETIC_GRID color tokens (primary-fixed: #c3f400)"
    - "Operator name input accepts text; DEPLOY button is present"
    - "Page background is #131313 (surface) with dot-grid pattern"
  artifacts:
    - path: "frontend/tailwind.config.ts"
      provides: "KINETIC_GRID design token system"
      contains: "primary-fixed"
    - path: "frontend/app/layout.tsx"
      provides: "Global shell with Space Grotesk, dark class on html"
      contains: "Space_Grotesk"
    - path: "frontend/app/page.tsx"
      provides: "Setup Screen (INITIALIZE_OPERATOR)"
      contains: "INITIALIZE_OPERATOR"
  key_links:
    - from: "frontend/tailwind.config.ts"
      to: "frontend/app/page.tsx"
      via: "Tailwind color tokens used in className"
      pattern: "primary-fixed"
---

<objective>
Migrate the frontend to the KINETIC_GRID cyber-noir design system and replace the home page with the Setup Screen (INITIALIZE_OPERATOR).

Purpose: Establish the visual identity for all Phase 2+ screens. After this plan, the game has a dark, electric-lime-accented aesthetic consistent with the Stitch design reference.
Output: New Setup Screen at `/`, Tailwind KINETIC_GRID tokens, Space Grotesk + Material Symbols fonts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-multiplayer-core/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Tailwind config with KINETIC_GRID design tokens</name>
  <files>
    frontend/tailwind.config.ts
  </files>
  <read_first>
    frontend/tailwind.config.ts
    .planning/phases/02-multiplayer-core/02-UI-SPEC.md
  </read_first>
  <action>
Update `frontend/tailwind.config.ts` to extend the theme with KINETIC_GRID tokens. The config must:

1. Add ALL color tokens from the UI-SPEC under `theme.extend.colors`:
```typescript
colors: {
  "surface":                  "#131313",
  "surface-container-lowest": "#0e0e0e",
  "surface-container-low":    "#1c1b1b",
  "surface-container":        "#201f1f",
  "surface-container-high":   "#2a2a2a",
  "surface-container-highest":"#353534",
  "surface-variant":          "#353534",
  "outline-variant":          "#444933",
  "outline":                  "#8e9379",
  "primary":                  "#ffffff",
  "secondary":                "#c6c6c7",
  "on-surface":               "#e5e2e1",
  "on-surface-variant":       "#c4c9ac",
  "primary-fixed":            "#c3f400",
  "primary-fixed-dim":        "#abd600",
  "on-primary-fixed":         "#161e00",
  "on-primary":               "#283500",
  "background":               "#131313",
  "inverse-surface":          "#e5e2e1",
  "secondary-container":      "#454747",
  "surface-bright":           "#3a3939",
  "surface-dim":              "#131313",
  "surface-tint":             "#abd600",
}
```

2. Add fontFamily:
```typescript
fontFamily: {
  "headline": ["Space Grotesk", "sans-serif"],
  "body":     ["Inter", "sans-serif"],
  "label":    ["Space Grotesk", "sans-serif"],
}
```

3. Override borderRadius to use sharp corners:
```typescript
borderRadius: {
  DEFAULT: "0px",
  sm: "0px",
  md: "0px",
  lg: "0px",
  xl: "0px",
  "2xl": "0px",
  full: "9999px",
}
```

4. Preserve the existing `content` config and `darkMode` setting. Add `darkMode: 'class'` if not present.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/tailwind.config.ts contains "primary-fixed"
    - frontend/tailwind.config.ts contains "#c3f400"
    - frontend/tailwind.config.ts contains "Space Grotesk"
    - frontend/tailwind.config.ts contains "borderRadius"
    - frontend/tailwind.config.ts contains "headline"
  </acceptance_criteria>
  <done>Tailwind configured with KINETIC_GRID color tokens, Space Grotesk/Inter font families, 0px border radius.</done>
</task>

<task type="auto">
  <name>Task 2: Update layout.tsx — fonts, dark mode, global shell</name>
  <files>
    frontend/app/layout.tsx
    frontend/app/globals.css
  </files>
  <read_first>
    frontend/app/layout.tsx
    frontend/app/globals.css
    .planning/phases/02-multiplayer-core/02-UI-SPEC.md
  </read_first>
  <action>
1. Update `frontend/app/layout.tsx`:
   - Add Space Grotesk and Inter via `next/font/google`
   - Add Material Symbols Outlined via a `<link>` tag in `<head>` (CDN — cannot use next/font for variable icon fonts)
   - Set `className="dark"` on `<html>` element
   - Apply `font-body bg-background text-primary` to `<body>`
   - Keep metadata title "Dots & Boxes"

```tsx
import type { Metadata } from "next";
import { Space_Grotesk, Inter } from "next/font/google";
import "./globals.css";

const spaceGrotesk = Space_Grotesk({
  subsets: ["latin"],
  variable: "--font-space-grotesk",
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "KINETIC_GRID — Dots & Boxes",
  description: "Real-time multiplayer Dots and Boxes",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`dark ${spaceGrotesk.variable} ${inter.variable}`}>
      <head>
        <link
          rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"
        />
      </head>
      <body className="min-h-screen bg-background text-primary font-body antialiased">
        {children}
      </body>
    </html>
  );
}
```

2. Update `frontend/app/globals.css`:
   - Keep Tailwind directives
   - Add `.material-symbols-outlined` font-variation-settings
   - Add `.dot-grid` utility class
   - Add `.kinetic-glow` utility

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

.material-symbols-outlined {
  font-variation-settings: 'FILL' 0, 'wght' 200, 'GRAD' 0, 'opsz' 24;
  font-family: 'Material Symbols Outlined';
  font-style: normal;
  display: inline-block;
  line-height: 1;
  text-transform: none;
  letter-spacing: normal;
  word-wrap: normal;
  white-space: nowrap;
  direction: ltr;
}

.dot-grid {
  background-image: radial-gradient(circle, #ffffff 1px, transparent 1px);
  background-size: 40px 40px;
}

.kinetic-glow {
  box-shadow: 0 0 15px rgba(204, 255, 0, 0.15);
}
```

**Note on next/font and CSS variables**: Tailwind fontFamily uses `"Space Grotesk"` as the string. Ensure the CSS variable approach maps correctly — if using `variable`, update `tailwind.config.ts` fontFamily to use `var(--font-space-grotesk)` instead of the string name.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/layout.tsx contains "Space_Grotesk"
    - frontend/app/layout.tsx contains "dark"
    - frontend/app/layout.tsx contains "Material+Symbols"
    - frontend/app/layout.tsx contains "bg-background"
    - frontend/app/globals.css contains "material-symbols-outlined"
    - frontend/app/globals.css contains "dot-grid"
  </acceptance_criteria>
  <done>Layout updated with Space Grotesk + Inter fonts, dark class, Material Symbols CDN link, and global CSS utilities.</done>
</task>

<task type="auto">
  <name>Task 3: Redesign home page as INITIALIZE_OPERATOR Setup Screen</name>
  <files>
    frontend/app/page.tsx
  </files>
  <read_first>
    frontend/app/page.tsx
    .planning/phases/02-multiplayer-core/02-UI-SPEC.md
  </read_first>
  <action>
Replace `frontend/app/page.tsx` with the KINETIC_GRID Setup Screen. The page must:

1. Be a Client Component (`'use client'`) — needs state for the name input
2. Have a fixed top navigation bar (TopAppBar)
3. Display centered `INITIALIZE_OPERATOR` form panel
4. Input: operator name (ENTER_ID)
5. CTA: DEPLOY_INITIAL_SEQUENCE button
6. On submit: store operator name in sessionStorage as `operatorName`, navigate to `/lobby`

```tsx
'use client';

import { useState } from 'react';
import { useRouter } from 'next/navigation';

export default function SetupScreen() {
  const [name, setName] = useState('');
  const router = useRouter();

  function handleDeploy() {
    if (!name.trim()) return;
    sessionStorage.setItem('operatorName', name.trim().toUpperCase());
    router.push('/lobby');
  }

  return (
    <div className="min-h-screen bg-background text-primary font-body overflow-hidden selection:bg-primary-fixed selection:text-on-primary-fixed">
      {/* TopAppBar */}
      <header className="fixed top-0 w-full z-50 flex justify-between items-center px-6 h-14 bg-surface border-b border-surface-variant/20">
        <div className="flex items-center gap-4">
          <span className="font-headline font-bold tracking-tighter text-primary text-lg">
            MONOCHROME_KINETIC_V1.0
          </span>
          <div className="h-4 w-px bg-outline-variant opacity-20" />
          <span className="font-label uppercase tracking-tight text-xs text-secondary">
            SYSTEM_STATUS: <span className="text-primary-fixed">OPERATIONAL</span>
          </span>
        </div>
        <div className="flex items-center gap-6">
          <span className="material-symbols-outlined text-sm text-secondary cursor-pointer hover:text-primary transition-colors">settings</span>
        </div>
      </header>

      {/* Main */}
      <main className="relative h-screen w-full flex items-center justify-center pt-14">
        {/* Dot grid background */}
        <div className="absolute inset-0 dot-grid opacity-[0.03] pointer-events-none" />

        {/* Panel */}
        <div className="relative w-full max-w-xl z-10 px-8">
          {/* Terminal header decoration */}
          <div className="flex items-center justify-between mb-4 px-2">
            <div className="flex gap-2">
              <div className="w-1.5 h-1.5 bg-primary opacity-20" />
              <div className="w-1.5 h-1.5 bg-primary opacity-20" />
              <div className="w-1.5 h-1.5 bg-primary opacity-20" />
            </div>
            <span className="font-label text-[10px] uppercase tracking-[0.2em] text-secondary opacity-50">
              AUTH_SEQUENCE_PENDING
            </span>
          </div>

          {/* Main panel */}
          <div className="bg-surface-container-high/40 backdrop-blur-xl border border-outline-variant/10 p-10 shadow-2xl">
            <div className="mb-12">
              <h1 className="font-headline text-3xl font-bold text-primary tracking-tighter mb-2">
                INITIALIZE_OPERATOR
              </h1>
              <p className="font-label text-[10px] uppercase tracking-widest text-secondary/60">
                ESTABLISHING ENCRYPTED CONNECTION TO GRID_SERVER_01
              </p>
            </div>

            <div className="space-y-12">
              {/* Name input */}
              <div className="space-y-3">
                <label className="font-label text-[10px] uppercase tracking-widest text-primary-fixed block">
                  INITIALIZING_OPERATOR_NAME
                </label>
                <div className="relative group">
                  <input
                    type="text"
                    placeholder="ENTER_ID"
                    value={name}
                    onChange={e => setName(e.target.value)}
                    onKeyDown={e => e.key === 'Enter' && handleDeploy()}
                    maxLength={24}
                    className="w-full bg-transparent border-0 border-b border-outline-variant/30 py-4 font-headline text-xl text-primary placeholder:text-surface-variant focus:ring-0 focus:outline-none transition-colors duration-200 focus:border-primary-fixed"
                  />
                  <div className="absolute right-0 top-1/2 -translate-y-1/2 flex items-center gap-2">
                    <span className="w-1 h-4 bg-primary-fixed animate-pulse" />
                  </div>
                </div>
              </div>

              {/* CTA */}
              <div className="pt-6 space-y-4">
                <button
                  onClick={handleDeploy}
                  disabled={!name.trim()}
                  className="w-full h-16 bg-primary-fixed text-on-primary-fixed font-headline font-bold uppercase tracking-widest flex items-center justify-center gap-3 hover:shadow-[0_0_20px_rgba(204,255,0,0.2)] transition-all duration-300 active:scale-[0.98] disabled:opacity-30 disabled:cursor-not-allowed"
                >
                  DEPLOY_INITIAL_SEQUENCE
                  <span className="material-symbols-outlined">double_arrow</span>
                </button>
                <div className="flex items-center justify-between px-2">
                  <div className="flex items-center gap-2">
                    <span className="w-2 h-2 bg-primary-fixed animate-ping" />
                    <span className="font-label text-[10px] text-secondary/60">SYNCING_ASSETS</span>
                  </div>
                  <span className="font-label text-[10px] text-secondary/40 uppercase">VER_2.0.0_STABLE</span>
                </div>
              </div>
            </div>
          </div>

          {/* Footer */}
          <div className="mt-8 flex justify-between items-start opacity-30">
            <p className="font-label text-[9px] leading-tight uppercase tracking-tighter max-w-[200px]">
              WARNING: ALL ACTIONS ARE LOGGED BY CENTRAL_VOID.
            </p>
            <div className="text-right">
              <p className="font-label text-[9px] uppercase tracking-tighter">KINETIC_GRID // TACTICAL DIVISION</p>
              <p className="font-label text-[9px] text-secondary/60">© 2026 GRID_OPS</p>
            </div>
          </div>
        </div>
      </main>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - frontend/app/page.tsx contains "INITIALIZE_OPERATOR"
    - frontend/app/page.tsx contains "primary-fixed"
    - frontend/app/page.tsx contains "DEPLOY_INITIAL_SEQUENCE"
    - frontend/app/page.tsx contains "operatorName"
    - frontend/app/page.tsx contains "use client"
    - frontend/app/page.tsx contains "/lobby"
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Home page replaced with KINETIC_GRID INITIALIZE_OPERATOR setup screen. Operator name stored in sessionStorage; navigates to /lobby on submit.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` exits 0 (no type errors)
- Home page (`/`) renders dark background (#131313) with INITIALIZE_OPERATOR heading
- Space Grotesk loads — all uppercase labels render in correct font
- DEPLOY button is disabled when input is empty, enabled when name is entered
- Tailwind config has `primary-fixed: #c3f400`
</verification>

<success_criteria>
KINETIC_GRID design system applied to frontend. Setup Screen renders at `/` with correct cyber-noir aesthetic.
</success_criteria>

<output>
After completion, create `.planning/phases/02-multiplayer-core/02-01-SUMMARY.md`
</output>
